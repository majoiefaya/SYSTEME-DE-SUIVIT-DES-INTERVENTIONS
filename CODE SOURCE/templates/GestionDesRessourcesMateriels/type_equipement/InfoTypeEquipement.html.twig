<!DOCTYPE html>

<title>TypeEquipement</title>

{% block body %}
    <h1>TypeEquipement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_equipement.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_equipement.Libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_equipement.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_equipement_index') }}">back to list</a>

    <a href="{{ path('app_type_equipement_edit', {'id': type_equipement.id}) }}">edit</a>

    {{ include('type_equipement/_delete_form.html.twig') }}
{% endblock %}
